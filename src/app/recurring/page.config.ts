import type { PageConfig } from "@/types";
import { PAGE_TITLES } from "@/components/PageStub/config";

export const RECURRING_PAGE: PageConfig = {
  path: "/recurring",
  title: PAGE_TITLES.recurring,
  ariaLabel: "Recurring Page",
};
