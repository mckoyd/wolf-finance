import type { PageConfig } from "@/types";
import { PAGE_TITLES } from "@/components/PageStub/config";

export const TRANSACTIONS_PAGE: PageConfig = {
  path: "/transactions",
  title: PAGE_TITLES.transactions,
  ariaLabel: "Transactions Page",
};
