import type { PageConfig } from "@/types";
import { PAGE_TITLES } from "@/components/PageStub/config";

export const BUDGETS_PAGE: PageConfig = {
  path: "/budgets",
  title: PAGE_TITLES.budgets,
  ariaLabel: "Budget page",
};
