import { PageConfig } from "@/types";
import { PAGE_TITLES } from "@/components/PageStub/config";

export const POTS_PAGE: PageConfig = {
  path: "/pots",
  title: PAGE_TITLES.pots,
  ariaLabel: "Pots page",
};
