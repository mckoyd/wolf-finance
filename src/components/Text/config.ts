export const ALIGN_CLASSES = {
  start: "alignStart",
  center: "alignCenter",
  end: "alignEnd",
  justify: "alignJustify",
} as const;

export const TEXT_DEFAULTS = {
  as: "p",
  variant: "preset-4",
  align: "start",
} as const;
